# نسخ للتطوير المحلي: cp .env.example .env

NODE_ENV=development
JWT_SECRET=change-me-in-production-use-long-random-string
PORT=4000

PUBLIC_BASE_URL=http://localhost:4000
APP_NAME=GOSTAAP

DELIVERY_RATE_PER_KM=2.5
DELIVERY_MIN_FEE=5
UPLOAD_DIR=./uploads
# محلي: backend/data/app.db — Render: أضف قرصاً دائماً واضبط DATABASE_PATH=/var/data/app.db
# DATABASE_PATH=./data/app.db

# ─── مدير المنصة (POST /api/auth/login ثم /api/admin/*) ───
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ─── SendGrid ───
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxx
MAIL_FROM=no-reply@yourdomain.com

ALLOW_DEV_OTP_LOG=false
OTP_EXPIRY_MINUTES=15
OTP_RESEND_COOLDOWN_SEC=60
OTP_MAX_ATTEMPTS=5
